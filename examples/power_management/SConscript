Import("env")

env.Default (
    env.Object(
        source = 'powerdown_example.c', 
        target = 'powerdown.rel'
    ),
    env.Object(
        source = 'idle_example.c', 
        target = 'idle.rel'
    )
)